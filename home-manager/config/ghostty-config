# Run `ghostty +show-config --default --docs` to view a list of all available config options and their default values.
# Additionally, each config option is also explained in detail on Ghostty's website, at https://ghostty.org/docs/config.
# Ghostty can reload the configuration while running by using the menu options or the bound key (Control + Shift + comma).

theme = Deep
background-opacity = 0.999
background = #000000
# background-blur = true # does not work, using blur-my-shell from gnome extensions
window-decoration = none
window-show-tab-bar = auto
maximize = true
gtk-toolbar-style = flat
gtk-wide-tabs = false
font-family = CaskaydiaCove Nerd Font Mono
font-size = 11
bell-features = no-audio,no-title,no-attention,no-system,no-border

keybind = clear
keybind = ctrl+shift+F5=reload_config
keybind = copy=copy_to_clipboard:mixed
keybind = paste=paste_from_clipboard
keybind = ctrl+shift+c=copy_to_clipboard
keybind = ctrl+shift+v=paste_from_clipboard
keybind = ctrl+==increase_font_size:1
keybind = ctrl++=increase_font_size:1
keybind = ctrl+-=decrease_font_size:1
keybind = ctrl+0=reset_font_size
keybind = shift+arrow_left=adjust_selection:left
keybind = shift+arrow_right=adjust_selection:right
keybind = shift+arrow_up=adjust_selection:up
keybind = shift+arrow_down=adjust_selection:down
# keybind = ctrl+shift+tab=previous_tab
# keybind = ctrl+tab=next_tab
# keybind = ctrl+shift+w=close_tab:this
# keybind = ctrl+shift+q=quit
# keybind = ctrl+shift+t=new_tab
# keybind = ctrl+shift+page_up=jump_to_prompt:-1
# keybind = ctrl+shift+page_down=jump_to_prompt:1
# keybind = ctrl+shift+f=start_search
# keybind = escape=end_search
keybind = ctrl+shift+p=toggle_command_palette

keybind = ctrl+alt+shift+l=clear_screen
